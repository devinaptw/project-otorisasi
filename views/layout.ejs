<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
		<title>Demo E-Commerce (Auth demo)</title>
		<link
			rel="stylesheet"
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
		/>
	</head>
	<body class="p-4">
		<nav class="mb-3">
			<a href="/" class="btn btn-sm btn-primary">Home</a>
			<a href="/products" class="btn btn-sm btn-secondary">Products</a>
			<a href="/orders" class="btn btn-sm btn-secondary">Orders</a>
			<% if (typeof user == 'undefined' || !user) { %>
			<a href="/login" class="btn btn-sm btn-success">Login</a>
			<a href="/register" class="btn btn-sm btn-info">Register</a>
			<% } else { %>
        <% if (user.role == 'admin') { %>
          <a href="/users" class="btn btn-sm btn-secondary">Users</a>
        <% } %>
			<span class="badge bg-light text-dark"
				>Logged as <%= user.username %> (<%= user.role %>)</span
			>
			<a href="/logout" class="btn btn-sm btn-danger">Logout</a>
			<% } %>
		</nav>
		<div class="container"><%- body %></div>
	</body>
</html>
